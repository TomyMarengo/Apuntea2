import { Card, ProfileForm } from '../components';

const Profile = () => {
  return (
    <section className="max-container center">
      <Card>
        <ProfileForm />
      </Card>
    </section>
  );
};

export default Profile;
