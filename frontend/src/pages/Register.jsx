import { Card, RegisterForm } from '../components';

const Login = () => {
  return (
    <section className="max-container center">
      <Card>
        <RegisterForm />
      </Card>
    </section>
  );
};

export default Login;
